spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:h2:mem:testdb;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=true;DATABASE_TO_UPPER=false
    username: sa
    password:
    driver-class-name: org.h2.Driver
debug: false
logging:
  level:
    com.wubo.test.dao: debug
